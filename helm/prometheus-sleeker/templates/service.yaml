apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-sleeker.fullname" . }}
  labels:
    {{- include "prometheus-sleeker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-sleeker.selectorLabels" . | nindent 4 }}
